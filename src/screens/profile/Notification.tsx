import {MainHeader, View} from '@components';
import React from 'react';

const Notification = () => {
  return (
    <View flex={1}>
      <MainHeader title="Notification" />
    </View>
  );
};

export default Notification;
